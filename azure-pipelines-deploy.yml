# Azure DevOps Pipeline for Guardian MVP Deployment
trigger:
- main

pool:
  vmImage: ubuntu-latest

variables:
  NODE_VERSION: '20.x'
  AZURE_WEBAPP_NAME: 'GUARDIAN-DEV'
  AZURE_WEBAPP_PACKAGE_PATH: 'deployment'
  NODE_ENV: 'production'

stages:
- stage: Build
  displayName: 'Build and Package'
  jobs:
  - job: Build
    displayName: 'Build Application'
    steps:
    - task: NodeTool@0
      inputs:
        versionSpec: $(NODE_VERSION)
      displayName: 'Install Node.js $(NODE_VERSION)'

    - script: |
        echo "Installing dependencies..."
        npm ci
        
        echo "Building frontend..."
        npm run build
        
        echo "Building server..."
        npm run build:server
        
        echo "Build artifacts:"
        ls -la dist
        ls -la dist-server
      displayName: 'Install dependencies and build'

    - task: ArchiveFiles@2
      displayName: 'Create deployment package'
      inputs:
        rootFolderOrFile: '$(System.DefaultWorkingDirectory)'
        includeRootFolder: false
        archiveType: 'zip'
        archiveFile: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId).zip'
        replaceExistingArchive: true

    - publish: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId).zip'
      artifact: drop

- stage: Deploy
  displayName: 'Deploy to Azure'
  dependsOn: Build
  condition: succeeded()
  jobs:
  - deployment: Deploy
    displayName: 'Deploy to Azure App Service'
    environment: 'production'
    strategy:
      runOnce:
        deploy:
          steps:
          - task: AzureWebApp@1
            displayName: 'Deploy to Azure Web App'
            inputs:
              azureSubscription: '$(AZURE_SERVICE_CONNECTION)'
              appType: 'webAppLinux'
              appName: '$(AZURE_WEBAPP_NAME)'
              package: '$(Pipeline.Workspace)/drop/$(Build.BuildId).zip'
              runtimeStack: 'NODE|20-lts'
              startUpCommand: 'npm start'

          - script: |
              echo "Verifying deployment..."
              STATUS_CODE=$(curl -s -o /dev/null -w "%{http_code}" https://$(AZURE_WEBAPP_NAME).azurewebsites.net/api/health)
              echo "Health check status code: $STATUS_CODE"
              if [ "$STATUS_CODE" != "200" ]; then
                echo "##vso[task.logissue type=error]Deployment verification failed with status code $STATUS_CODE"
                exit 1
              fi
              echo "Deployment verified successfully!"
            displayName: 'Verify deployment'
