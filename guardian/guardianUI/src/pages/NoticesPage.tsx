import { Stack, Typography } from "@mui/material"

export const NoticesPage = () => {
    return (
        <Stack mt={10} ml={2} pt={2} spacing={2}>
        <Typography variant='h4' className='displayLabel'>Notices</Typography>
        <Typography variant='subtitle1'>At some point there will be requirements for what goes here and we'll do it lol</Typography>
        </Stack>
    )
}