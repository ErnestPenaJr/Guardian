# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage
/.auth/
/playwright-report/
/test-results/

# production
/build
/dist

# environment variables
.env
.env.*

# misc
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# windsurf rules
.windsurfrules

# deployment artifacts
manual-deploy.zip
simple-deploy/

# codex
AGENTS.md
/.codex

# claude
CLAUDE.md
/.claude

# debug & test artifacts
/.playwright-mcp/
/screenshots/
/snapshots/

# gstack security scan artifacts — contain live exploit payloads, MUST NOT ship to production
.gstack/

# Postgres migration data dump — contains real PII + password hashes, MUST NOT be committed.
# The schema file (postgres/01_schema.sql) is safe to commit; the seed clone is not.
postgres/02_seed.sql
