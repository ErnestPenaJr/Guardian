# azure-pipelines.yml
trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '20.x'
  displayName: 'Install Node.js'

- script: |
    npm install
    npm run build
    echo "Frontend build completed. Listing dist directory contents:"
    ls -la dist
  displayName: 'Install and build frontend'

- script: |
    mkdir -p deployment
    echo "Creating deployment directory structure"
  displayName: 'Prepare deployment directory'

- script: |
    echo "Copying frontend build to deployment/dist"
    cp -r dist deployment/
    ls -la deployment/dist
  displayName: 'Copy frontend build to deployment'

- script: |
    echo "Copying azure-server.cjs to deployment root"
    cp azure-server.cjs deployment/server.js
  displayName: 'Copy azure-server.cjs to deployment'

- script: |
    echo "Copying deployment-package.json to deployment/package.json"
    cp deployment-package.json deployment/package.json
    cat deployment/package.json
  displayName: 'Copy deployment package.json'

- script: |
    echo "Copying Prisma schema and related files"
    if [ -f "prisma/schema.prisma" ]; then
      mkdir -p deployment/prisma
      cp -r prisma/* deployment/prisma/
      echo "Prisma schema copied"
      ls -la deployment/prisma/
    else
      echo "No Prisma schema found - skipping"
    fi
  displayName: 'Copy Prisma files'

- script: |
    echo '[config]
    SCM_DO_BUILD_DURING_DEPLOYMENT=true' > deployment/.deployment
    cat deployment/.deployment
  displayName: 'Create .deployment file'

- script: |
    cd deployment
    echo '<?xml version="1.0" encoding="utf-8"?>
    <configuration>
      <system.webServer>
        <handlers>
          <add name="iisnode" path="server.js" verb="*" modules="iisnode"/>
        </handlers>
        <rewrite>
          <rules>
            <rule name="NodeInspector" patternSyntax="ECMAScript" stopProcessing="true">
              <match url="^server.js\/debug[\/]?" />
            </rule>
            <rule name="StaticContent">
              <action type="Rewrite" url="public{REQUEST_URI}"/>
            </rule>
            <rule name="DynamicContent">
              <conditions>
                <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="True"/>
              </conditions>
              <action type="Rewrite" url="server.js"/>
            </rule>
          </rules>
        </rewrite>
        <security>
          <requestFiltering>
            <hiddenSegments>
              <remove segment="bin"/>
            </hiddenSegments>
          </requestFiltering>
        </security>
        <httpErrors existingResponse="PassThrough" />
      </system.webServer>
    </configuration>' > web.config
  displayName: 'Create web.config'

- script: |
    cd deployment
    npm install
    echo "Installed server dependencies in deployment directory"
    
    # Copy pre-generated Prisma client from local build
    if [ -d "../node_modules/.prisma/client" ]; then
      echo "Copying pre-generated Prisma client..."
      mkdir -p node_modules/.prisma
      cp -r ../node_modules/.prisma/client node_modules/.prisma/
      echo "Pre-generated Prisma client copied successfully"
      ls -la node_modules/.prisma/client/
    else
      echo "No pre-generated Prisma client found - attempting generation"
      if [ -f "prisma/schema.prisma" ]; then
        echo "Generating Prisma client..."
        npx prisma generate
        echo "Prisma client generated"
      else
        echo "No Prisma schema found - skipping client generation"
      fi
    fi
    
    ls -la node_modules
  displayName: 'Install server dependencies and copy Prisma client'

- script: |
    cd deployment
    zip -r ../app.zip .
    cd ..
    du -h app.zip
  displayName: 'Create deployment package'

- task: AzureWebApp@1
  inputs:
    azureSubscription: 'AzureSP-Dev'
    appType: 'webApp'
    appName: 'GUARDIAN-MVP'
    package: '$(System.DefaultWorkingDirectory)/app.zip'
    runtimeStack: 'NODE|20-lts'
  displayName: 'Deploy to Azure Web App'